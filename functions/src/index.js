const { onCall } = require('firebase-functions/v2/https');

// TODO(phase-4): callNextClient — chair assignment transaction
// TODO(phase-4): markAttending — called -> attending
// TODO(phase-4): markFinished — attending -> finished
// TODO(phase-4): markAbsent — called -> absent
// TODO(phase-4): reactivateClient — absent -> waiting (priority: true)
// TODO(phase-5): sendRegistrationConfirmation — MSG 1
// TODO(phase-6): setEventStatus — inactive -> active -> closed

module.exports = {};
